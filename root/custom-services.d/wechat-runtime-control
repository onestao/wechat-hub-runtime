#!/usr/bin/with-contenv bash
set -euo pipefail

PYTHON_BIN="/lsiopy/bin/python3"
if [ ! -x "$PYTHON_BIN" ]; then
    PYTHON_BIN="$(command -v python3)"
fi

exec "$PYTHON_BIN" /scripts/wechat/wechat_runtime_control.py
